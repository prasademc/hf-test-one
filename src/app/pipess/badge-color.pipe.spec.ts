import { BadgeColorPipe } from './badge-color.pipe';

describe('BadgeColorPipe', () => {
  it('create an instance', () => {
    const pipe = new BadgeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
