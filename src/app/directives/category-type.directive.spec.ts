import { CategoryTypeDirective } from './category-type.directive';

describe('CategoryTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new CategoryTypeDirective();
    expect(directive).toBeTruthy();
  });
});
